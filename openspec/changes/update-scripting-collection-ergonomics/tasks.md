## 1. Implementation
- [x] 1.1 Add spec deltas for collection-aware condition/runtime behavior and new expression helpers
- [x] 1.2 Consolidate shared collection resolution utilities for lists and JSON-backed values
- [x] 1.3 Implement `in` / `not in` plus structural collection emptiness/length semantics
- [x] 1.4 Add `list`, `compact`, `distinct`, `push_unique`, `trim_all`, `lower_all`, and `upper_all`
- [x] 1.5 Update `foreach` to accept expression-backed collections
- [x] 1.6 Add focused tests for operators/helpers/foreach expressions/end-to-end flow
- [x] 1.7 Update documentation and at least one bundled sample
- [x] 1.8 Run focused verification and `openspec validate update-scripting-collection-ergonomics --strict --no-interactive`
