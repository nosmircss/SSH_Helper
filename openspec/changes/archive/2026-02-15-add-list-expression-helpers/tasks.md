## 1. Runtime support
- [x] 1.1 Add `unshift(list, value)` to prepend values for list expressions
- [x] 1.2 Add destructive list removal helpers `pop(list)` and `shift(list)` for writable list variables
- [x] 1.3 Add non-destructive list helpers `first(list)`, `last(list)`, `indexof(list, value)`, and `concat(list1, list2, ...)`

## 2. Documentation and tests
- [x] 2.1 Update `SCRIPTING.md` Supported Expressions and examples for new list helpers
- [x] 2.2 Add set-command unit tests covering list helper behavior and edge cases
- [x] 2.3 Run scripting-focused tests and verify pass
