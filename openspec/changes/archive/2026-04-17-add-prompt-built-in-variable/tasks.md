## 1. Implementation
- [x] 1.1 Add focused runtime/editor regression coverage for `${_prompt}`.
- [x] 1.2 Expose `${_prompt}` from the scripting runtime and editor built-in metadata.
- [x] 1.3 Keep `_prompt` out of preconnect host-variable propagation.
- [x] 1.4 Update scripting docs and spec deltas for the new built-in.
